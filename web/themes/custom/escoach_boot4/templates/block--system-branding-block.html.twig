{#
/**
 * @file
 * Bootstrap Barrio's theme implementation for a branding block.
 *
 * Each branding element variable (logo, name, slogan) is only available if
 * enabled in the block configuration.
 *
 * Available variables:
 * - site_logo: Logo for site as defined in Appearance or theme settings.
 * - site_name: Name for site as defined in Site information settings.
 * - site_slogan: Slogan for site as defined in Site information settings.
 */
#}
{% set attributes = attributes.addClass('site-branding') %}
  {% if site_logo or site_name %}
   <div class="container"><a href="{{ path('<front>') }}" title="{{ 'Home'|t }}" rel="home" class="navbar-brand mainLogo">
      {% if site_logo %}
        <img src="{{ site_logo }}" alt="{{ 'Link to home page of ES-Coach'|t }}" class="img-fluid d-inline-block align-top" />
      {% endif %}

    </a>
  {% endif %}

    <div class="d-inline-block align-top site-name-slogan">
    <h3 class="sizeSlogan">A project of ThinkWork<br />Institute for Community Inclusion<br />University of Massachusetts Boston</h3>
    </div></div>
