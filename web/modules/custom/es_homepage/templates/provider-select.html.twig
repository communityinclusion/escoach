<div class="provider-select">
  <label>Choose a provider</label>
  <select name="provider">
    {% for provider in data.providerList %}
    <option value="{{ provider }}">{{ provider }}</option>
    {% endfor %}
  </select>
</div>
